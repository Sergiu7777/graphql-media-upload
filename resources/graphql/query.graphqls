type Query {
    getImageById(fileId: String!): Image
    getImages: [Image]
}

type Image {
    id: ID!
    fileName: String
    fileSize: Int
    contentType: String
    downloadLink: String
    uploadedBy: String
    upVotes: Int
    downVotes: Int
}